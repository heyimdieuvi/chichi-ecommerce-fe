import React from 'react'
import { defaultTheme } from '../config/themes'

const Login: React.FC = () => {
  return (
    <div>
      <h1 style={{ color: defaultTheme.colors.primary }}>Login</h1>
    </div>
  )
}

export default Login
